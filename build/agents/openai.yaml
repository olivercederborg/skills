interface:
  display_name: "Build"
  short_description: "Implement an agreed plan in small, verified steps"
  default_prompt: "Use $build to implement the agreed plan."
policy:
  allow_implicit_invocation: true
