interface:
  display_name: "Grounding"
  short_description: "Check a claim against installed code and docs"
  default_prompt: "Use $grounding to check whether this claim holds."
policy:
  allow_implicit_invocation: true
