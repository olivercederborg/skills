interface:
  display_name: "Ready Check"
  short_description: "Verdict on whether a branch, PR, or stack is done"
  default_prompt: "Use $ready-check to tell me if this work is done."
policy:
  allow_implicit_invocation: true
