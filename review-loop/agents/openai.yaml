interface:
  display_name: "Review Loop"
  short_description: "Claude and Codex review, verify, fix, repeat"
  default_prompt: "Use $review-loop to review this change with Claude and Codex."
policy:
  allow_implicit_invocation: true
